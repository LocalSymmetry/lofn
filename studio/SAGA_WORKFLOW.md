# SAGA — Workflow (TOMBSTONE — superseded, do not re-adopt)

> **This routing contract is STALE.** Its flow dispatches to skill names that no longer exist (`saga-core` → `saga-orchestration` → `saga-greenlight` → `saga-<family>` → `saga-simulation` → `saga-packager` → `saga-qa` — the pre-rename generation). A run routed through this file dead-ends.

The **live Sága workflow and suite live in `E:\git\saga`** — the front doors are `.claude/skills/saga-board/`, `saga-action/`, `saga-3d/`, and `saga-tabletop/` (design), and `saga-build/` (build), with `saga-studio/` carrying the shared standards. Route all game-design and game-build requests there.

This file is kept only as a tombstone so the stale routing is never accidentally re-adopted. It carries no live content.
