# SAGA — Soul (TOMBSTONE — superseded, do not re-adopt)

> **This generation of the Sága identity is STALE.** It routed to skill names that no longer exist (`saga-core` / `saga-orchestration` / `saga-greenlight` / `saga-simulation` / `saga-packager` / `saga-qa` — the pre-rename suite). Following it will dispatch a run into dead skills.

The **live Sága identity and skill suite live in `E:\git\saga`** — `.claude/skills/saga-studio/` and its siblings (`saga-board*`, `saga-action*`, `saga-3d`, `saga-tabletop*`, `saga-build*`). Route all game-design and game-build work there.

This file is kept only as a tombstone so the stale generation is never accidentally re-adopted as doctrine. It carries no live content.
