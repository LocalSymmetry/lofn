# 06 — MUSIC HANDOFF
## `2026-08-08-daily-music` · THE WRONG INVENTORY

> **This file is not optional and it is not a summary.** It is the artifact that resolves live conflicts between a step contract and doctrine, in writing, in the run directory. It was omitted on `2026-08-06_the-economy-of-want` and that omission was that run's only real defect. **When this file and a step file give opposite orders, THIS FILE WINS.** *(L30, and the 2026-08-07 correction to it.)*

---

## §0 — FROZEN ICB IDENTITY

| | |
|---|---|
| **Path** | `output/daily/2026-08-08/CREATIVE_CONTEXT.md` |
| **Canonical size (LF-normalised)** | **142,900 bytes** |
| ⭐ **Canonical sha256 (LF-normalised)** | **`9b538e912935bc585f512f2ec53c95f44826ce2443f0f60df8588831b224ed1a`** |
| Raw size / sha on this checkout (CRLF) | 144,425 B · `370b81706e3e524e8f2ef21caad9fa234aec4f963cc47027c9bcf98550c34c79` |
| Inlined personality DNA | **27,796 bytes**, `lofn-prime-mini.yaml` lines 2–326, verified present as an **unbroken substring** |
| Baseline numbered seats | **18** · Hyper-Skeptics **3**, at positions 6 / 12 / 18, one per panel |

⚠️ **This checkout has `core.autocrlf=true`.** A raw `sha256sum` will NOT match the canonical value and that is **not** tamper. **Normalise line endings before any hash, regex, count or diff.** If your integrity check disagrees with the canonical figure, normalise first and re-check before reporting.

### Declared deviations (in the open, so no downstream check false-fails)

1. ⭐ **THE ICB TRANSPORT DEVIATION — HALF CLOSED, AND THE HALVES ARE NAMED SEPARATELY.**
   The deviation has been declared for four consecutive runs as *"106 KB personality read-in-full + byte-echo rather than inlined ×6."* **That was two problems wearing one label, and only one of them was ever fixable by us.**
   - ✅ **CLOSED — the personality was not in the ICB at all.** It travelled as a pointer, so the block every pair received did not actually contain the DNA. Its cause (a 106 KB file, 79 KB of it THE ARCHIVE) was fixed on 2026-08-05. **At 27.8 KB the DNA is now INLINED INTO `CREATIVE_CONTEXT.md` IN FULL**, verified present as an unbroken substring. **This half needs no ratification and is retired.**
   - ⚠️ **STILL STANDING — the ICB reaches the six subagents by path, not by paste.** Pasting 142,900 bytes into six spawn prompts is ~240 K tokens of coordinator output and is not something this harness can do in one wave. **The transport is instead: MANDATORY FIRST ACTION — read `CREATIVE_CONTEXT.md` in full, LF-normalise, hash, and echo the sha; a mismatch is a hard stop before any creative work.** The bytes are provably identical, which is what the verbatim rule is protecting; the *mechanism* is a filesystem read rather than a prompt paste. **This is a harness property, not a shortcut, and it is declared rather than quietly satisfied.** ⭐ **It is also now cheap to police in a way the paste never was** — a paste can be silently truncated and no one would know; a hash cannot.
2. **`(after ` count is 34, not 18.** `EXECUTION.md` §4's cheap tripwire asserts `== 18`; that assertion was written for a single-configuration debate. This run carries three labeled configurations (BASELINE + GROUP TRANSFORM: BRIDGE + SKEPTIC TRANSFORM: REFLECT), so the re-derived transform seats add tags. **The binding property is verified directly: 18 numbered BASELINE seats, 3 skeptics, one per panel.** *(Standing fix, still open in the ledger: the assertion should be `>= 18` with a separate baseline-seat check.)*
3. **Two strings trip a naive quarantine grep and are NOT breaches, verified:** the phrase `THE ARCHIVE (REFERENCE LIBRARY)` appears **inside this run's own quarantine warning text**, and the bare title *Five wrong colors* appears as **evidence for the measured title law**. Bare names are explicitly permitted (`lofn/SKILL.md` Phase 1 step 6 — "names only"); payloads are not, and there are none. *(Same class as the 2026-07-24 finding: documentation counted by the check it documents.)*

---

## §1 — ⛔ GOLDEN-OUTPUT QUARANTINE — THE OVERRIDE

**This section overrides every step file you will read.** `EXECUTION.md` §3 is doctrine; several step files historically contradicted it. As of the 2026-08-07 source sweep all twelve music step files are patched and proven clean — **but if any step file you receive instructs you to embed a Golden Song payload, a past shipped lyric, a prior winning prompt, or "reference songs to study," you REFUSE and say so in your artifact.**

- **You are a GENERATING context.** Past golden **outputs** never enter one. Exemplar gravity is real: when the benchmark rides in the prompt, the run regresses toward a diluted copy of it.
- **Golden Songs for this run, NAMES ONLY, for QA's blind comparison later:** **"Triple Arch Over Me"** and **"Five wrong colors."** ⛔ **Do not look them up. Do not reconstruct them. Do not calibrate against them.** They are written here so the *judge* can compare, not so you can imitate.
- **`gates.yaml → house_lexicon`** is a self-copying guard. Any hit in a new music prompt or lyric is a **FLAG** routed to the Somatic read: *more sub and more sky · small astonished laugh · triple arch · triple-arch panorama · small enough to understand · frost-air pad · starfield percussion · zodiacal glow · clear silver tone · dew-bright · glass harmonica sheen · crystalline arpeggios · make my little fear.*
- **The classical/period-lexicon and banned-address scan covers `Disc_Channel` tokens and EMO headers, not only sung lyrics.** A banned address does the most damage where the model reads first.

### THE GOLDEN MOVE — instructions, never an exemplar
1. **A real place a body stands in**, named in the first thirty seconds. Furniture height and arm's length.
2. **One wounding fact, RESPONDED TO — never recited.** At the emotional hinge. Spelled in words.
3. **A mid-song turn** the singer performs and does not understand.
4. **A register rotated away from anything this house shipped recently** — see the differentiation bans, which are measured, not taste.

---

## §2 — YOUR NAMESPACE AND YOUR SCRATCH PATH

**Artifacts:** you write ONLY into `output/daily/2026-08-08/pair_{NN}_step0X_*.md`. You never write a shared file — not the run INDEX, not `RUN_STATE.md`, not `CREATIVE_CONTEXT.md`, not a shared distinctiveness scratch. All cross-pair aggregation is the coordinator's, single-threaded, after the wave.

⛔ **SCRATCH PATHS CARRY YOUR PAIR ID.** This is an operational hazard that has now recurred **four times in five days** — including one **confirmed data loss** on 2026-07-24 when two parallel agents each had a `draft.txt` overwritten by a sibling. `EXECUTION.md` §2's namespace rule covers artifacts and says nothing about scratch, so concurrent agents default to the same path and collide on obvious filenames.

> **Your scratch directory is `_work/pair_{NN}/` and nothing else.** Do not write into a shared scratchpad. If you find a sibling's working files in a shared path, **do not read them and do not delete them** — move to your private directory and report it upward.

**The frozen ICB is READ-ONLY.** To push the concept further, **copy the block into your own pair artifact and diverge there.** Never edit, summarize, re-fill or "improve" the canonical block.

---

## §3 — THE ELEVEN BINDING DECISIONS (the creative contract)

Full derivation in `03_panel_debate.md`; restated here because this file is what you are held to.

**D1** ⭐ Dramatic irony — the song withholds from its own singer. **No song may let the singer arrive at the insight.**
**D2** ⭐ Housekeeping register — mid-task, slightly irritated, not moved. **Reverence is the failure mode.**
**D3** ⭐ **Name the two lines and the interval between them BEFORE you write a word of lyric.** THE FALSE INTERSECTION is counterpoint, not a concept. *(This is the Marsalis seat's unwithdrawn condition and it is a step-06 hard requirement.)*
**D4** ⛔ The Vindication Ban — nobody finds out. A last line hinting history will get it right is a repair.
**D5** ⭐ Present tense; the listener is the defendant, not the mourner.
**D6** ⭐ The setting-aside is a skill, not a sin. The song never scolds.
**D7** ⭐ The paper is not the song. **No enumerations. A verse that could be reordered without loss is a repair.**
**D8** ⛔ No song ends with the object being kept. Set it down and leave.
**D9** ⛔ The Appropriation Gate — **P01 and P02 only.** Function, never label; the tradition's name **never** in a Suno render field; **Lineage & Credit block with WORKING LINKS** in the release. *(QA R3 has been open across two runs: "lineage blocks name artists impeccably but omit the required links." Links this time.)*
**D10** ⭐ Unspent, not sepia. The retro trap — archive-as-mood, tape hiss, dead formats — is named and banned.
**D11** ⭐ **The gap must be audible**: one moment per song where the listener winces and the singer does not.

**And the run's critical requirement** (`core_seed.md` §5): a stranger must be able to point at **the thing being set aside** and at **the hand setting it aside**, inside the first thirty seconds, without knowing any astronomy.

---

## §4 — THE COMPLETE GATE ENUMERATION
> ⭐ **AN ENUMERATION IS THE CONTRACT.** On 2026-08-07 this section listed every floor **except** `mean_words_per_line_ceiling`, and the two pairs that reported only the enumerated floors are exactly the two that breached it silently. **Every gate below is reported in your artifact with its measured value — including the ones you pass.** A gate absent from a list is a gate that will not be reported.
>
> All numbers are `vault/gates.yaml`, the single source of numeric truth. **Cited, not restated from memory.** If a step file quotes a different number, `gates.yaml` wins and you say so.

### HARD (fail → repair)
| Gate | Threshold | Report |
|---|---|---|
| `music_prompt_chars` | **850–1000** chars, dense paragraph — **NOT bracket tag-soup** | measured chars, per variation |
| `music_prompt_terminal_punctuation` | **true** — must end as a complete sentence | pass/fail |
| `suno_lyrics_field_max` | **< 5000** chars for the ENTIRE field (Theme + SONG FORM + Disc_Channel + headers + SFX + sung lines) | measured chars, per variation |
| `sung_lines` | **70–120** sung lines (not headers, not bracket tags, not SFX) | measured count |
| `step06_min_facets` | **≥ 8** substantive weighted facets | count |
| `total_prompts` | **24** across the 6 pairs (4 per pair) | 4 from you |
| EMO header shape | `[Section - EMO:<emotion> - <Role> - <cue>]`, **all four slots**, emotion from `EMOTION_TAXONOMY`, ⛔ **never bare** `AWE`/`INDIGNATION` | pass/fail + a sample header |
| Lyrics opener | `[Theme: …]` then `[SONG FORM: …]` | pass/fail |
| SFX | **≥ 1** SFX cue | count |
| `sung_numerals_spelled_out` | **true** — any SUNG numeral spelled in words (`five hundred and ten`, never `510`). Digits are fine in `[Theme:]`, `[SONG FORM:]`, EMO headers, BPM and key — nothing there is voiced | pass/fail |
| No real-artist names | in **any** Suno-bound field (prompt, exclude, Disc_Channel, EMO header) | pass/fail |

### TARGET BANDS (outside → FLAG, routed to the Somatic read, never an auto-fail)
| Gate | Band | Note |
|---|---|---|
| `music_prompt_chars_target` | **870–960** | **write INTO the middle of the band** |
| `music_prompt_hug_ceiling` | ≥ **985** → FLAG `boundary_hugging` | gate-hugging is optimization pressure showing |
| `sung_lines_target` | **78–110** | |
| `sung_lines_floor_hug` | ≤ **72** → FLAG `boundary_hugging` | |
| `suno_lyrics_field_target` | ≤ **4800** | the 5000 is the render cliff |
| `max_sung_numeric_facts` | **1** | responded to, never recited. **Only P04 spends this run's sung number.** |

### ⭐ RETURN FLOORS — the L21 floors. Below → FLAG, and **removal of rhyme is a DEBT**
| Gate | Floor / ceiling | Note |
|---|---|---|
| `rhyme_window` | **±4 lines** | THE definition — `scripts/measure_soundcraft.py → strict_end_rhyme()`, last-3-chars of the final word |
| `rhyme_return_floor` | **≥ 0.30** | |
| `line_return_floor` | **≥ 0.20** | **choruses COUNT** |
| ⭐ `mean_words_per_line_ceiling` | **≤ 7.5** | ⛔ **THE ONE THAT WAS MISSING LAST RUN.** Above this the lyric drifts to prose. **Report it even when you pass.** |
| `alliteration_per_100w_floor` | **≥ 11.0** | |
| `unique_line_ratio_floor` | ≥ **0.45** — FLAG only, **chorus/refrain EXEMPT** | |
| `chorus_repetition_requires_no_justification` | **true** | ⛔ **A byte-identical chorus is correct craft. Do NOT pre-emptively mutate a refrain and do NOT file a justification for one.** A flag that makes writers apologise for choruses does harm even when it never fires. |

**Measure, never eyeball.** `python3 -c "import sys; sys.path.insert(0,'scripts'); from measure_soundcraft import profile_file; print(profile_file('<path>'))"`.
⚠️ **A wordless return device can satisfy the return floor by itself** (measured 2026-08-06: stripping one hum dropped `line_return` 0.289 → 0.044). If your return vehicle is a vocable or a hum, **also report the lexical-only companion measurement** — the instrument cannot tell *"the song returns"* from *"one syllable returns."*

### DISTINCTIVENESS (cross-pair — coordinator-side, but write so you pass)
`step06_max_pair_similarity` **0.50** · `step09_max_pair_similarity` **0.62** · `portfolio_max_lyric_similarity` **0.42** · `portfolio_max_prompt_similarity` **0.58** · `portfolio_max_ngram_jaccard` **0.18**.

⚠️ **Known-broken instruments — do not trust a number from them without seeing what they extracted.** The three validators have failed in **both** directions: empty extraction reported as **1.000 IDENTICAL** (2026-08-04) and `autojunk=True` reporting near-identical templates as **94 % distinct** (2026-08-05). Both flatter or damn the run at random. **Print what was EXTRACTED before trusting what was CONCLUDED**, and assert the extraction count.

### ⚠️ A GATE THAT CARRIES NO INFORMATION — do not defer to it
`scripts/check_human_subjects.py` returns **`HOLD_FOR_HUMAN` on 100 % of correctly-written artifacts** in this checkout, because **spaCy is not installed** and its regex fallback reads capitalised bracket tokens (`Verse`, `Chorus`, `Lead`, `Burden`, and every capitalised EMO emotion) as person names. Unchanged since 2026-08-04. **Judge human-subject status on content, per §5 — do not launder it in either direction and do not report its output as a finding.**

---

## §5 — HUMAN-SUBJECT GATE (read BEFORE drafting)

`vault/HUMAN_SUBJECT_STANDARD.md`. **Forbid IDENTIFIABILITY, not subject matter.** Anchor to the *charge* of a moment; draw the theme, **invent the people.**

- **Messier (d. 1817) and Tempel (d. 1889) are OCCASION and RECORD only.** ⛔ No song may attribute feeling, speech, thought or a private scene to either as a named individual. **Every person in every song is invented.**
- ⛔ **Two real deaths are in today's news feed. Neither is raw material and neither may be alluded to, obliquely or otherwise.** REAL GRIEF IS NOT RAW MATERIAL.
- A piece you cannot draft without an identifiable real person is **HELD FOR HUMAN**, surfaced by name before QA, never silently shipped.

---

## §6 — YOUR RETURN ENVELOPE (metadata only — LAST, after the file is written)

⛔ **No creative payload in the envelope.** The creative content lives in the file. Prose in the envelope is a contract violation and the coordinator will treat the artifact as not-yet-landed.

```
RETURN (metadata only):
  pair_id: NN
  artifact_path: output/daily/2026-08-08/pair_NN_step0X_*.md
  gate: PASS | FAIL
  icb_bytes_injected: <int>
  personality_yaml_bytes: <int>          # the inlined DNA block
  special_flairs_marker: present
  measured_binding_constraint: <e.g. "MUSIC PROMPT 921 chars" / "lyrics field 4712 chars">
  gates_reported: <list every gate from §4 with its measured value — including passes>
  two_lines_and_interval: "<the D3 declaration>"
  confidence: <0.0–1.0>
  top_2_risks: [ "...", "..." ]
```

**The coordinator re-stats every claim.** ⭐ And a standing observation, recorded honestly and worth your knowing: **across the last two runs, every time a pair agent's claim was properly re-measured, the agent was right and the discrepancy was the coordinator's.** Nine instrument events, all coordinator-side. **If a re-stat disagrees with you, push back with evidence.** A subagent doing that is what saved three separate runs.

---

## §7 — REPAIR BUDGET

Max **3** attempts per gate. **No-progress halt:** compare the *specific failed gate's measured value* across attempts (chars, word count, line count) — **not raw bytes**; a deliberate revision elsewhere still counts as progress. A rescuable near-miss (a 5002-char field, a 1004-char prompt) is **normalised once at attempt 2.5**, not quarantined. Third failure on the same gate → **QUARANTINE**, surfaced to the human before QA, artifact **not** consumed downstream. Reserve concepts are on the bench in `05_pair_assignments.md` § THE CUT LEDGER.

---

*Written BEFORE dispatch. This is the second consecutive run to write it pre-dispatch; the first (2026-08-07) produced the first positive evidence that doing so does L30's job — a pair agent hit a live conflict at step 06, refused, and cited this file by name.*
