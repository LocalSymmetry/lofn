# 06 — Vision Handoff — "The Answered Room" (NightCafe #1365, 2026-07-09)

## READ FIRST
- **Run directory:** `output/2026-07-09_nightcafe_no_theme/`
- **The ICB is frozen:** `CREATIVE_CONTEXT.md` in this directory is the Immutable Continuity Block — inject **verbatim, no summarization**, at the head of EVERY step (coordinator 00–05 and every pair step 06–10). No agent edits it; a pair that wants to diverge copies into its own `pair_NN_*` artifact and diverges there.
- **Step contracts:** `skills/image/steps/0X_*.md` (canonical step files 00–10). Modality carrier already filled (this run's `CREATIVE_CONTEXT.md`).
- **Numeric gates:** `vault/gates.yaml` — image scene ≥80 words; 24 total prompts; banned imperative openers (Create/Design/Make/Render/Depict); banned haze words (ethereal/dreamlike/whimsical/gentle light/soft glow/magical/delicate); step00 taxonomy 50/50/50/50 and ≥2000 bytes.
- **Serialized writes:** pair agents write ONLY `pair_{NN}_*` files; the coordinator alone writes shared artifacts (RUN_STATE.md, INDEX) after waves land.

## ORCHESTRATOR (Phase-1 record)
- **Personality:** LOFN-ArtCore (all six pairs; ACCESSIBLE = warmer register, AMBITIOUS = intense register). YAML verified + embedded in the ICB.
- **Panel:** Masters of Wonderful Strange (library), v2 re-derived; final working ledger = 18 voices, one Hyper-Skeptic per panel (Estrangement Officer after Magritte · Straight-Print Enforcer after Adams · Spectacle Prosecutor after Žižek). Transforms run: **Focus (group) → Bridge (skeptic)**.
- **Locked mood:** answered longing — relief still checking the door.
- **Synthesis law:** THE ANSWERED ROOM (three-beat read: beauty → impossibility → splinter).
- **Artifacts:** `01_seed_lineage.md` · `02_golden_seed.md` · `03_panel_debate.md` · `04_metaprompt.md` · `05_pair_assignments.md` · `CREATIVE_CONTEXT.md` (frozen) · this handoff.

## GOLDEN SEED
- Full marked seed: `02_golden_seed.md` (lineage SEED 14 × SEED 19; dangerous permission; non-negotiables; the five fresh axes A–E).
- **THE GOLDEN MOVE (distilled generative instructions — no past output payloads ride with generators):**
  1. **Stand somewhere the body knows** — every scene keeps one contact-noun the viewer's hand remembers (brass latch, wet cobble, bread and paraffin, wool coat).
  2. **Answer one named lack** — the splinter names what was missing (empty chair, unboarded case, dark window); the guest answers it without erasing it.
  3. **Turn mid-frame** — the three-beat read is the turn: beauty first, impossibility discovered, debt found last.
  4. **Rotate the register away from our recent work** — recent Lofn image sets were news-anchored, systems-facing, indignant. This run is AWE: domestic sublime, witnessed repair. Do not reuse recent devices (reliquary/control-room/public-square/threshold variation frames are RETIRED for this run).

## PAIR AGENTS (dispatch contract)
- **Slots:** `05_pair_assignments.md` — six slots, barbell 3+3, Latin-square axis corners, per-slot composition law + angle-derivation lens. Step 05 selects concepts INTO slots (with runner-up rationale + cut ledger).
- **Chain:** full-chain-per-pair, steps 06→07→08→09→10, one subagent per pair, 6 in parallel. Steps 07/09/10 are the NO-SKIP editorial spine.
- **Every pair prompt contains, in order:** role line → the ENTIRE `CREATIVE_CONTEXT.md` verbatim → this pair's slot + selected concept + prior artifact → the step contract(s) → self-check gates → describe-render self-check (one inline pass: predict the literal rendered frame in 2–3 sentences, name the one way it would render generic, repair once if drifting) → the metadata-only RETURN block (pair_id, artifact_path, gate, icb_bytes_injected, personality_yaml_bytes, special_flairs_marker, measured_binding_constraint, confidence, top_2_risks).
- **Coordinator re-stat:** on each return — file exists, non-trivial, binding constraint recomputed from disk, ICB prefix unbroken substring, `(after ` count == 18. Only then `done` in RUN_STATE.md.
- **Repair ceiling:** max 3 attempts; no-progress halt on the failed gate's measured value; QUARANTINE terminal surfaces "N of 6 pairs broke open at step X" with the cut-ledger REPLACE/REDIRECT options before QA.

## QA CONTRACT
- **Judge in clean context** (fresh subagent; artifact + ICB + gate spec only), adversarial framing; different tier where available; verdicts SHIP / REPAIR / FAIL; a structurally complete but generic piece is REPAIR — SOUL LOSS.
- **Structural gates (per prompt):** noun-first present-tense ≥80 words; no imperative opener; banned haze words absent; medium + physical signature in first third; no living-artist names; no children; adults only; emotion shown not named.
- **Seed gates (per prompt):** the splinter present and nameable; one un-posable tell; one contact point; two-temperature palette from the slot's corner; ~10% rest zone; figure+guest one silhouette at 128px; three-beat read traceable; title candidates ≤5 words; one-breath retell stated in the pair guide.
- **Set gates:** 24/24 prompts; no axis-option reused across pairs; angle labels unique per pair; set spans hours (≥1 bright pair); ArtCore provable per pair (visual-world sentence + signature device + seed weirdness); cross-pair distinctiveness at 06/09/10 (no shared skeletons, no shared reveal-engines — check the concept pool for engine-twins, not just line-twins).
- **Somatic read:** the three Hyper-Skeptics' laws hold in the FINAL prompts (splinter survived; no haze; earned warmth) — if the set reads as the Hallmark aisle, it is REPAIR regardless of structure.
- **Deliverable:** ranked portfolio; top pick + runner-up surfaced with why-this-wins; per-artifact saves under `output/images/` per OUTPUT.md; run INDEX last; run-health footer (pairs shipped/quarantined, gate retries, qa repairs).

*Provenance: Phase 1 close-out, coordinator inline. Self-critique: the handoff bans our recent variation frames — the coordinator must also watch that the six NEW derivation lenses don't quietly rebuild them.*
