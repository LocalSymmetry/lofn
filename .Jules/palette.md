## 2024-05-23 - Streamlit Button Hierarchy
**Learning:** Streamlit's `icon` parameter (v1.34+) and `type='primary'` provide a native way to establish visual hierarchy without custom CSS hacks.
**Action:** Use `:material/icon_name:` syntax for cleaner, accessible button icons instead of emoji in text labels.
